# Graph-Based Influencer Detection

## Setup

1. Install dependencies:
   pip install -r requirements.txt

2. Run data extraction:
   python modules/module0_data_extraction.py

3. Run pipeline:
   python main.py

4. Run dashboard:
   streamlit run app.py